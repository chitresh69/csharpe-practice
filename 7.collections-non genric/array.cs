/*class Demo
{
    static void Main(string[] args)
    {


        string[] myArray = { "apple", "banana", "cherry" };

        //foreach (var item in myArray)
        //{
        //    Console.WriteLine($"Element at index {item} ");
        //}

        //foreach (var item in myArray.Select((index, element) => new
        //{
        //    Index = index,
        //    Element = element,
        //}))
        //{
        //    Console.WriteLine($"Element at index {item.Index}: {item.Element}" );
        //}

        Array.Resize(ref myArray, 8);
        Console.WriteLine(myArray.Length);

        foreach (var item in myArray)
        {
            Console.WriteLine($"Element at index {item} ");
        }

    }
}

*/